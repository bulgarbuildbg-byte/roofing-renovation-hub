

## Подреждане на текстовете в hero секцията на „Нов покрив" — ляво подравняване като останалите страници

### Файл: `src/pages/services/NewRoofPage.tsx` (редове 200-237)

### Промени

**1. Container — от центриран към ляво подравнен** (ред 201)
- `max-w-4xl mx-auto text-center` → `max-w-4xl` (без `mx-auto`, без `text-center`)

**2. Заглавие h1** (ред 202)
- Остава както е, но вече ще е ляво-подравнено

**3. Подзаглавие p** (ред 205)
- Премахва се `mx-auto` от `max-w-2xl mx-auto`

**4. Badges** (ред 209)
- `justify-center` → без `justify-center` (ляво подравнени)

**5. Бутони** (ред 228)
- `justify-center` → без `justify-center`

**6. Breadcrumb навигация** — добавя се преди h1 (както в RoofRepairPage):
```
Начало / Услуги / Изграждане на нов покрив
```

### Резултат
- Текстът, бутоните и badges ще са подравнени вляво — както на RoofRepairPage и другите service страници

