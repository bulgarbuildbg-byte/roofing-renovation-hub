## Цел
Създаване на акаунт за `radnev.alexander@gmail.com` с роля "Маркетинг" и достъп до целия CRM панел, без права за изтриване и без управление на екипа.

## Какво съществува вече (проверено)
- Всички DELETE политики в базата (запитвания, оферти, договори, обекти, статии, обаждания и др.) са ограничени само до роля `admin` — маркетингът няма да може да трие нищо на ниво база.
- `user_roles` (добавяне/премахване на хора и роли) също е само за `admin` — маркетингът не може да добавя хора или да премахва администратора.
- Страницата "Управление на екипа" (`/admin/staff`) вече е защитена с `requireAdmin`.
- Всички останали админ страници (лийдове, аналитика, маркетинг атрибуция, оферти, договори, обекти, статии и т.н.) са достъпни за всеки с роля — тоест маркетингът ще ги вижда.

## Стъпки
1. Създаване на потребителя през админ функцията за екип: имейл `radnev.alexander@gmail.com`, парола `Radnev02`, роля `marketing`, потвърден имейл (без нужда от имейл верификация).
2. Проверка, че записът се появява в `user_roles` с роля `marketing` и че профилът се създава автоматично.
3. Преглед на админ интерфейса: бутоните за изтриване (кошчета) да са скрити за не-администратори там, където още се показват — за да не се натиска бутон, който така или иначе ще върне грешка.
4. Кратка проверка, че страницата "Управление на екипа" не се показва/не е достъпна за маркетинг ролята.

## Технически детайли
- Създаването минава през edge функцията `create-team-member` (service role + проверка, че извикващият е admin).
- Не се променят RLS политики — текущите вече дават точно исканите права.
- Ако някъде в UI-а има delete бутон без проверка `isAdmin`, се добавя такава проверка (само презентационна промяна).

## Забележка за сигурност
Паролата беше изпратена в чата. Препоръчително е след първо влизане потребителят да я смени.
